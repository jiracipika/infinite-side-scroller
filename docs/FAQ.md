# Dashverse — Troubleshooting FAQ

Generated by qwen-worker (local Qwen3-8B) 2026-08-20, reviewed by Hermes.
Maps common failure symptoms to the verify script that catches them.

## Troubleshooting FAQ

**Symptom:** Level progression math not working → run `npm run verify:progression` (progression math validation)
**Symptom:** Achievements not unlocking → run `npm run verify:achievements` (achievement rule check)
**Symptom:** Save slot data lost → run `npm run verify:saves` (save slot integrity)
**Symptom:** HUD shows wrong hearts/coins → run `npm run verify:hud` (HUD invariants check)
**Symptom:** Game feels laggy on Retina → run `npm run verify:performance` (profiler/DPR optimization)
**Symptom:** CI build config broken → run `npm run verify:codemagic` (CI config validation)
**Symptom:** Touch controls dead on mobile → run `npm run verify:touch` (touch controls test)

If the targeted script passes but the symptom persists, run the full ladder:
`npm run verify` (all verify scripts + typecheck + lint + tests).
See docs/SCRIPTS.md for the complete script reference.
