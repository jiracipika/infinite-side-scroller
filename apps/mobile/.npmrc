# Expo SDK 55's compatible package set currently contains an upstream React peer-range conflict.
# Keep CI reproducible: npm ci uses the committed lockfile rather than attempting a different peer resolution.
legacy-peer-deps=true
